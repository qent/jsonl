# JSONL Schemas

`claude-code-jsonl-line.v1.schema.json` describes one raw Claude Code JSONL history line.

The schema version should change when the viewer starts recognizing new top-level events, message content blocks, or important payload shapes. It intentionally allows additional properties so new Claude Code releases can still be opened before the schema is updated.
